using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class flutuateWindRotation : MonoBehaviour
{
    public float RangeFlu = 3.0f;
    private float RandomRange = Random.Range(0.0f, 5.0f);
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
